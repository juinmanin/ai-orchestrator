import './globals.css'

export const metadata = {
  title: 'AI Quota Orchestrator',
  description: 'Smart orchestrator to maximize your free AI platform quotas',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
